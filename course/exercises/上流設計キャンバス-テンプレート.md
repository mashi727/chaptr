# 上流設計キャンバス（空テンプレート）

> コースの持ち帰り成果物。**着手前に、後で変えると高くつく決定だけ**を書き切る1枚。
> 全部を埋めようとしないこと。⑧（可逆性）で「片道ドア」に入れたものだけ、いま真剣に決める。
> 各章を見終わるたびに1マスずつ埋め、S12で全体を CLAUDE.md に変換する。

プロジェクト名（仮）: ______________________
記入日: ______________________

---

## ① 課題 — 誰の・どんな痛みを・どの頻度で
> 一文で。理想は自分が当事者（dogfooding）。

- 痛み（一文）:
- 誰の:
- 頻度 × 一回の苦痛:

## ② 成功条件 — 何ができたら「解決」か（Done）
> 観測可能な形で。v1に入れないものを書く方が大事。

- v1のDone（観測可能な形で）:
- v1に入れない（v2以降）:

## ③ スコープ境界 — やる / やらない / 将来やるかも
> 特に Out を、理由付きで書く。別の利用者向けの塊は別リポジトリに割る。

- In:
- Out（理由付き）:
- Later:
- 別リポジトリに割る塊はあるか:

## ④ 中核データと契約 — 一番安定させたいもの
> 不変条件を先に書く。「同時に起きたら？」「空だったら？」「順序は？」を問う。

- 中核データ:
- 不変条件（3つ以上）:
  1.
  2.
  3.
- この契約の唯一の実装（関数/モジュール）:

## ⑤ 分解方針 — 単一目的の単位と層
> 状態 / UI仲介 / 重い処理 で分ける。可変(UI)と不変(データ)の境界。

- 単一目的の部品:
- 層（状態 / 仲介 / 重処理）:
- 可変・不変の境界:

## ⑥ 配布・運用制約 — 誰が・どのOSで・どう入手
> 配布は工程でなく制約。依存の同梱/要求が実装を縛る。

- 誰が使う:
- 対象OS:
- 入手経路:
- 依存（外部バイナリ等）は同梱 or 要求:
- 配布形態の要件（例：コンソール非表示）:

## ⑦ 技術選定 — 選択と「乗り換える条件」
> 選定より「いつ見切るか」を先に。

- 主要技術:
- 乗り換える条件（〜になったら〜へ）:

## ⑧ 可逆性マップ — 片道ドア / 両開きドア
> 「3ヶ月後に変えると何が倒れるか」で仕分ける。片道だけ着手前に慎重に。

| 片道ドア（慎重に・着手前） | 両開きドア（即決・後で直す） |
|---|---|
|  |  |
|  |  |
|  |  |

## ⑨ AI協働設計 — Claude Codeとどう組むか
> CLAUDE.md方針／握る判断／ガードレール。

- 自分が握る判断（3つ）:
- AIに委ねる作業:
- ガードレール（ブランチ規律 / 判断ログ / まずデータを見る 等）:

## ⑩ 名前とアイデンティティ
> 決めるのは安いが変えるのは高い。名前の揺れはスコープの揺れの症状。

- 確定名（プロダクト / パッケージ / エントリポイント）:
- 内部名と外部名を分けるか:
- → 確定したら ⑧ の片道ドア側に置く

---

## 使い方メモ
- 空欄のまま走り出してよいのは、⑧で「両開きドア」と判断できたマスだけ。
- 決定が変わったら、その理由を判断ログ（worklog）に残す。キャンバスは石版ではない。
- 合言葉：**決定を、コードでなく文章で外在化せよ。**
